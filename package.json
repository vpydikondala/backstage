{
  "name": "backstage-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "yarn tsc && backstage-cli build",
    "build:backend": "backstage-cli backend:bundle --build-dependencies",
    "tsc": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@backstage/cli": "^1.27.0"
  }
}
