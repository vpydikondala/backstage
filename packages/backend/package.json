{
  "name": "backend",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "backstage-cli backend:bundle --build-dependencies",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@backstage/backend-defaults": "^1.0.0"
  }
}
