{
  "name": "backend",
  "private": true,
  "main": "dist/index.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "backstage-cli backend:bundle --build-dependencies",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@backstage/backend-defaults": "^1.27.0"
  }
}
