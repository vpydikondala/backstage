# Core FastAPI dependencies
fastapi
uvicorn[standard]

# HTTP requests (httpx for async requests, requests for sync)
httpx
requests

# For environment variable support
python-dotenv

# For working with data validation and parsing
pydantic

# Async support with external APIs
aiohttp

# Kubernetes API client (for interacting with Kubernetes resources)
kubernetes

# PostgreSQL support (used for database connections)
psycopg2-binary

# SQLAlchemy ORM for working with databases
SQLAlchemy

# For working with YAML (for configurations, etc.)
pyyaml

# For JWT token handling (auth, token validation)
PyJWT

# For production environments, often you'd want a WSGI server (optional if you are using `uvicorn`)
gunicorn

# Optional - if your app needs CORS
starlette

# Optional - for background tasks handling
celery

# For logging (optional, might be included for better logging management)
loguru
