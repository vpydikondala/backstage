app:
  title: Backstage (IDP PoC)
  baseUrl: http://backstage.local

backend:
  baseUrl: http://backstage.local
  listen:
    port: 7007
  cors:
    origin: http://backstage.local
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  database:
    client: pg
    connection:
      host: postgresql.backstage.svc.cluster.local
      port: 5432
      user: backstage
      password: BACKSTAGE_DB_PASSWORD
      database: backstage

integrations:
  github:
    - host: github.com
      token: GITHUB_PERSONAL_ACCESS_TOKEN

auth:
  environment: development
  providers:
    github:
      development:
        clientId: GITHUB_OAUTH_CLIENT_ID
        clientSecret: GITHUB_OAUTH_CLIENT_SECRET

techdocs:
  builder: 'local'
  publisher:
    type: 'local'

catalog:
  rules:
    - allow: [Component, API, Location, Resource, System, Domain, Group, User]
  locations:
    - type: url
      target: https://github.com/YOUR_GH_ORG?type=all
      rules:
        - allow: [Component, System, Resource, API]
    - type: file
      target: ./templates

kubernetes:
  serviceLocatorMethod:
    type: 'multiTenant'
  clusterLocatorMethods:
    - type: 'config'
      clusters:
        - name: minikube
          url: https://kubernetes.default.svc
          authProvider: 'serviceAccount'
          skipTLSVerify: true
  customResources: []

argocd:
  baseUrl: http://argocd-server.argocd.svc.cluster.local
  appLocatorMethods:
    - type: 'annotations'
